import React from 'react';
import ReactDOM from 'react-dom/client';

import { SaveMyWalletApp } from './SaveMyWalletApp';

import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <SaveMyWalletApp />
    </React.StrictMode>
);
