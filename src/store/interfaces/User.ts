export interface User {
    id: number;
    username: string;
    email: string;
    role: string; // TODO: Change for an enum
    createdAt: string; // TODO: change for datetime
    updatedAt: string; // TODO: change for datetime
}
