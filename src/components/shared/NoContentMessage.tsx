import { Alert } from '@mui/material';

export const NoContentMessage = () => {
    return (
        <>
            <Alert severity='warning'>No hay contenido que mostrar.</Alert>
        </>
    );
};
